#include <iostream>
#include <fstream>
#include <vector>
#include <string>
using namespace std;
struct Alumno {
    int codigo;
    string nombre;
    string apellido;
    string carrera;
    string estado;
};
void AgregarAlumno(vector<alumno> &ListaAlumnos,int codigo, const string &nombre, const string &apellido, const string &carrera, const string &estado)
Alumno nuevoAlumno;
nuevoAlumno.codigo;
nuevoAlumno.nombre;
nuevoAlumno.apellido;
nuevoAlumno.carrera;
nuevoAlumno.estado;
ListaAlumno.push_back(nuevoAlumno);
